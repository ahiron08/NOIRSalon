# local development overrides (dev only — never commit a real .env)
NODE_ENV=development
PORT=5000
CLIENT_URL=http://localhost:3000
API_URL=http://localhost:5000/api/v1
# Use local MongoDB for development (change to Atlas URI when deploying)
# Standard connection (bypasses SRV DNS lookup on restricted networks)
MONGODB_URI=mongodb+srv://qwerewera_db_user:frogviperx101@cluster0.8u52bja.mongodb.net/?appName=Cluster0
JWT_SECRET=dev_noir_salon_super_secret_change_me
JWT_EXPIRES_IN=7d
JWT_COOKIE_EXPIRES_IN=7
ADMIN_NAME=NOIR Admin
ADMIN_EMAIL=admin@noirsalon.in
ADMIN_PASSWORD=ChangeMe123!
PAYMENT_PROVIDER=cash

JWT_SECRET=5zitVpQ4Fhk-7mz2hH0Rfrmxg7-fxFgTYgQTxYAUpQb-b2BLO-sPacxfznlff_iL
JWT_EXPIRES_IN=7d
JWT_COOKIE_EXPIRES_IN=7

# ---- Email (Nodemailer) ----
# SMTP host/credentials for transactional mail (welcome, verify,
# password reset, appointment confirmations, newsletters).
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM="NOIR SALON <no-reply@noirsalon.in>"

# ---- Cloudinary (media storage) ----
CLOUDINARY_CLOUD_NAME=erbkdvxv
CLOUDINARY_API_KEY=358647549621324
CLOUDINARY_API_SECRET=QE4_gPwSSeeD4biUFCUmPJcq-NU

# ---- Payment providers (abstract layer — fill when going live) ----
PAYMENT_PROVIDER=cash
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# ---- Instagram (live gallery feed: posts + reels) ----
# OPTIONAL — when set, the Gallery page pulls recent posts/reels straight from
# the Instagram Graph API and they auto-update. Leave blank to use the curated
# items marked "Instagram import" in the admin Gallery panel instead.
# Create a long-lived token via a Facebook app (Instagram Graph API, "Instagram
# Login" / "Generate access token" with the `instagram_business_basic` perm):
#   https://developers.facebook.com/docs/instagram-platform/instagram-api-with-instagram-login
INSTAGRAM_ACCESS_TOKEN=
INSTAGRAM_USERNAME=noir.salon

# ---- Google (reviews + maps) ----
GOOGLE_PLACES_API_KEY=
GOOGLE_MAP_EMBED=

